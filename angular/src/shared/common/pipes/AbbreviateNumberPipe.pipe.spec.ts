/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AbbreviateNumberPipePipe } from './AbbreviateNumberPipe.pipe';

describe('Pipe: AbbreviateNumberPipee', () => {
  it('create an instance', () => {
    let pipe = new AbbreviateNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
