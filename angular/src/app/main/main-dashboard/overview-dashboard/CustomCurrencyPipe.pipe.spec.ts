/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CustomCurrencyPipePipe } from './CustomCurrencyPipe.pipe';

describe('Pipe: CustomCurrencyPipee', () => {
  it('create an instance', () => {
    let pipe = new CustomCurrencyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
