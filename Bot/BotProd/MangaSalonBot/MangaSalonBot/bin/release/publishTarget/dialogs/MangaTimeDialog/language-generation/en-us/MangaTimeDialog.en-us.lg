[import](common.lg)

# TextInput_Prompt_wettny()
[Activity
    Attachments = ${json(ButtonList(dialog.TimeCaption,dialog.Result.content.result))}
    InputHint=${conversation.InputHint}
     Summary=${conversation.Summary}
]